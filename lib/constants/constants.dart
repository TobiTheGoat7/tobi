import 'package:flutter/material.dart';
import 'package:flutter_screenutil/flutter_screenutil.dart';

class Constants {
  //padding;

  static const designWidth = 428.0;
  static const designHeight = 926.0;

  static const appLeadingWidth = 16.0;

  static const mainScreenPadding = EdgeInsets.symmetric(horizontal: 28.0);
  static final dialogPadding =
      EdgeInsets.symmetric(horizontal: 37.0.w, vertical: 50.0.h);

  static final dialogBorderRadius = BorderRadius.circular(15.0);

  static final dialogHeight = 283.0.h;

  static final dialogWidth = 350.0.w;

  static const animationDuration = Duration(milliseconds: 300);
}
